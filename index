<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SageDash</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { 
      font-family: 'Segoe UI', sans-serif; 
      background: #f3f1ef;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .container { max-width: 500px; margin: 0 auto; text-align: center; }
    .logo { width: 280px; margin: 0 auto 1.5rem; animation: pulse 2s infinite; }
    @keyframes pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.05); } }
    h1 { font-size: 2.5rem; color: #3e3e3c; margin: 1rem 0; }
    p { font-size: 1.1rem; color: #3e3e3c; margin: 1.5rem 0; }
    .buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
    a, button {
      padding: 14px 32px; font-weight: 600; border-radius: 999px; cursor: pointer; text-decoration: none; min-width: 160px;
    }
    .primary { background: #9ca58a; color: white; }
    .primary:hover { background: #433931; }
    .secondary { background: white; color: #945e5b; border: 2px solid #d1fae5; }
    .secondary:hover { border-color: #945e5b; }
  </style>
</head>
<body>
  <div class="container">
    <img src="/logo.png" alt="SageDash Logo" class="logo">
    <h1>Expert Parenting Coach for Teen Communication</h1>
    <p>Bond Closer. Rise Together.</p>
    <div class="buttons">
      <a href="/onboarding.html" class="primary">Start Onboarding</a>
      <button class="secondary">Sign In</button>
    </div>
  </div>
</body>
</html>
